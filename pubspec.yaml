name: blackjack21_v2
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.5.0+6

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.6.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  audioplayers: ^6.8.1
  firebase_core: ^4.12.1
  firebase_auth: ^6.5.6
  google_sign_in: ^7.2.0
  # Direct dependency because widgets/google_signin_button_web.dart calls
  # web_only.renderButton() — authenticate() throws UnimplementedError on web.
  google_sign_in_web: ^1.1.3
  cloud_firestore: ^6.7.1
  url_launcher: ^6.3.2
  flutter_local_notifications: ^22.2.0
  shared_preferences: ^2.5.3
  # Direct dependency because the daily-bonus reminder builds TZDateTime
  # values itself when scheduling through flutter_local_notifications.
  timezone: ^0.11.1
  share_plus: ^13.3.0
  # Only for `usePathUrlStrategy()` in main.dart, so the web build resolves
  # `/join/CODE` from the real URL path instead of the default `#/` hash
  # fragment — needed for the invite deep link to work on web.
  flutter_web_plugins:
    sdk: flutter
  firebase_analytics: ^12.5.0
  firebase_crashlytics: ^5.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Turns on `flutter gen-l10n`, which reads l10n.yaml + lib/l10n/*.arb and
  # generates the typed AppLocalizations class this app's 10 supported
  # locales use.
  generate: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/sfx/
    # Asset folders are not recursive — the spoken number words need their own
    # entry or they are left out of the bundle.
    - assets/sfx/num/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Bundled rather than fetched at runtime. `google_fonts` downloaded all
  # three families on first launch, so a cold start on a slow connection drew
  # the entire game in the platform fallback face and then reflowed — the
  # first thing a new player ever saw was the app changing its mind about what
  # it looks like. These ship inside the APK, so frame one is correct and the
  # app no longer needs the network to render text.
  #
  # Sora is the upstream variable font (there are no static instances in
  # google/fonts); AppText.sora pins its `wght` axis via `fontVariations`.
  fonts:
    - family: Sora
      fonts:
        - asset: assets/fonts/Sora-Variable.ttf
    - family: Instrument Serif
      fonts:
        - asset: assets/fonts/InstrumentSerif-Italic.ttf
          style: italic
    - family: Space Mono
      fonts:
        - asset: assets/fonts/SpaceMono-Regular.ttf
        - asset: assets/fonts/SpaceMono-Bold.ttf
          weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
